Libraries required:
mtcnn==0.1.0
tensorflow==2.3.1 (vgg face works stable with this version)
keras==2.4.3
keras-vggface==0.6
keras_applications==1.0.8

use python 3.7

Upload a photo you want to check in the folder.
Copy the photo file path, and run the test.py
you will be asked for the path of the image, paste it by pressing ctrl + shift + v
you will get the actor/actress name in the terminal and a small photo will be shown
